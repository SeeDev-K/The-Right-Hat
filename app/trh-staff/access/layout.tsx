export const metadata = {
  title: 'TRH staff access',
  robots: { index: false, follow: false },
}

export default function StaffAccessLayout({ children }: { children: React.ReactNode }) {
  return children
}
